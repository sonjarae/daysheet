<%= form_with(model: venue, local: true) do |form| %>
  <% if venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venue.errors.count, "error") %> prohibited this venue from being saved:</h2>
      <ul>
      <% venue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :venue_name %>
  </div> 
  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :venue_address %>
  </div> 
    <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :venue_phone %>
  </div> 
  <div class="field">
    <%= form.label :contact %>
    <%= form.text_field :contact, id: :venue_contact %>
  </div> 
  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :venue_email %>
  </div> 

<!--
    address picker / etc.
    t.string "address"
    t.string "phone"
    t.string "website"
-->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
